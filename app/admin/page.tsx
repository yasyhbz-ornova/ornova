import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function AdminPage() {
  return <PlaceholderPage title="مدیریت" text="این بخش فقط جایگاه آینده مدیریت است و در این اسپرینت به داده یا سرویس متصل نیست." />;
}
